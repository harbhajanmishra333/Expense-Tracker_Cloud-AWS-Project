// Configuration for testing without authentication
const config = {
  api: {
    endpoint: 'https://dxrdjqrw54.execute-api.us-east-1.amazonaws.com',
    useAuth: false // Set to true when you want to use Cognito authentication
  }
};

export default config;
